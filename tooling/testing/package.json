{
  "name": "@reforce/tooling-testing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./windows-signal-harness": "./dist/windows-signal-harness.js"
  },
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && tsc -p test/tsconfig.json",
    "test": "bun test test",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "dependencies": {
    "@reforce/primitives": "workspace:*",
    "@swc/helpers": "catalog:",
    "execa": "^10.0.1",
    "strip-ansi": "^7.2.0",
    "tempy": "^3.2.0"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@reforce/tooling-rslib": "workspace:*",
    "@reforce/tooling-tsconfig": "workspace:*",
    "@rslib/core": "catalog:",
    "@types/bun": "catalog:",
    "radashi": "^12.9.1",
    "typescript": "catalog:"
  }
}
