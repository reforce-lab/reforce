{
  "name": "@reforce/primitives",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && tsc -p test/tsconfig.json",
    "test": "bun test test",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "dependencies": {
    "@swc/helpers": "catalog:"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@reforce/tooling-tsconfig": "workspace:*",
    "@rslib/core": "catalog:",
    "@types/bun": "catalog:",
    "fast-check": "^4.9.0",
    "typescript": "catalog:"
  }
}
