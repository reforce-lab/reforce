# 复制成 .env 再改。Reforce 依次读取 .env、.env.local，真实的环境变量优先级最高。
# 变量名由 config/ 里的前缀和字段名推出来：webServer + port → WEB_SERVER_PORT。

WEB_SERVER_PORT=3000

# 配上就会对写接口做校验（请求要带 x-api-key），留空等于关掉。
# APP_API_KEY=change-me-please
