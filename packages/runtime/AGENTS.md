# AGENTS.md
<!-- Generated by APM CLI from distributed .apm/ primitives -->
<!-- Build ID: 68230723a56f -->
<!-- APM Version: 0.26.0 -->

## Files matching `packages/runtime/**`

- 本包 dist 经 `reforce:dev-runtime` / `reforce:production-runtime` 模块替换**原样进入用户 dev/production 产物**（ADR 0009，#191）：`rslib.config.ts` 必须保持显式 `minify: false`，注释随 JS 原样出线。
- 全包源码的注释里禁止出现 `@reforce/compiler`、`createCompiler`、`PARSER_SYNTAX_ERROR`、`yuku-parser`（Issue #22）；断言在 cli 的 `it/integration/production-build.spec.ts`（「产物不含构建期依赖」）。挂在纯类型声明上的注释随类型一起擦除，不在此限。
- 依赖表只允许 `@reforce/context`、`@swc/helpers`、`radashi`；`@reforce/compiler`、`@rsbuild/*`、`@reforce/primitives` 连 devDependencies 都不得出现——「运行时不可达构建期代码」由包依赖边与 typecheck 保证，取代 cli 时代的手工可达集枚举。
- 逐 subpath 导出、无 `.` 主入口、无 barrel：cli 每条跨包 import 必须精确可 grep，「可达集 = import 图」的不变量由 exports 面维持。
- `dev-ipc` 与 production ack 的两个 guard 语义不同（#191 R12），不得"顺手统一"。

---
*This file was generated by APM CLI. Do not edit manually.*
*To regenerate: `apm compile`*
