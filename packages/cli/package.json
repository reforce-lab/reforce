{
  "name": "@reforce/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "reforce": "./dist/reforce.js"
  },
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && tsc -p test/tsconfig.json && tsc -p it/tsconfig.json",
    "test": "bun test --timeout 300000 test it",
    "test:recovery": "bun test it/recovery/project-lease.spec.ts it/recovery/directory-transaction.spec.ts",
    "test:recovery:evidence": "bun test it/recovery/project-lease.spec.ts it/recovery/directory-transaction.spec.ts --reporter=junit --reporter-outfile=recovery-junit.xml",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "dependencies": {
    "@reforce/compiler": "workspace:*",
    "@reforce/context": "workspace:*",
    "@reforce/primitives": "workspace:*",
    "@rsbuild/core": "^2.1.9",
    "@swc/helpers": "catalog:",
    "commander": "^15.0.0",
    "radashi": "^12.9.1"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@reforce/tooling-testing": "workspace:*",
    "@reforce/tooling-rslib": "workspace:*",
    "@reforce/tooling-tsconfig": "workspace:*",
    "@rslib/core": "catalog:",
    "@types/bun": "catalog:",
    "execa": "^10.0.1",
    "typescript": "catalog:"
  }
}
