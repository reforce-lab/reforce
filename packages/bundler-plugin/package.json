{
  "name": "@reforce/bundler-plugin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bundler plugin that compiles and publishes Reforce starter meta from library builds.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./rsbuild": {
      "types": "./dist/rsbuild.d.ts",
      "default": "./dist/rsbuild.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rslib build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.node.json && tsc -p it/tsconfig.json",
    "test": "vitest run",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "dependencies": {
    "@reforce/compiler": "workspace:*",
    "@reforce/primitives": "workspace:*",
    "@swc/helpers": "catalog:",
    "publint": "^0.3.22",
    "unplugin": "^3.3.0"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@reforce/context": "workspace:*",
    "@reforce/tooling-rslib": "workspace:*",
    "@reforce/tooling-testing": "workspace:*",
    "@reforce/tooling-tsconfig": "workspace:*",
    "@reforce/tooling-vitest": "workspace:*",
    "@rslib/core": "catalog:",
    "@types/node": "catalog:",
    "esbuild": "^0.28.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
