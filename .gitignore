# deps
node_modules/

# build output
dist/
.reforce/
*.tsbuildinfo

# e2e fixture starter 的 dist 是手写的已构建面（reforce lib 的输入），必须提交
!/e2e/fixtures/*/dist/
!/e2e/fixtures/*/dist/**

# turbo
.turbo/

# test / coverage
coverage/
/e2e/e2e-junit.xml
/packages/cli/recovery-junit.xml

# logs
*.log

# os
.DS_Store

# ide
.idea/
.vscode/
!.vscode/settings.json

# env
.env
.env.*
!.env.example

# apm —— 真相源头是 apm.yml + .apm/，以下为编译/安装产物
# （以 / 开头只匹配根目录；apm.lock.yaml 同 bun.lock 一样需要提交，不在此列）
/AGENTS.md
/CLAUDE.md
.worktrees
.kimi-code
.agents
.claude
.codex
apm_modules/
