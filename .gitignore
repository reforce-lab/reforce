# deps
node_modules/

# build output
dist/
.reforce/
*.tsbuildinfo

# e2e fixture starter 的 dist 是手写的已构建面（reforce lib 的输入），必须提交
!/e2e/fixtures/*/dist/
!/e2e/fixtures/*/dist/**

# 真 starter 包的 meta 三件套由 `reforce lib` 随 build 产出（#153），与 dist 同为构建产物。
# 用通配而不是逐包罗列：新增引擎包时漏写一行就会把构建产物提交进来（e2e 夹具在 /e2e 下，不受影响）。
# 两层通配对应 packages/<分组>/<包>/：写成一层不是「少忽略几个文件」，而是整条规则失配、
# 三件套全部变成待提交，同时 biome 会连带把生成的 json 一起检查（它默认读 .gitignore）。
/packages/*/*/reforce-meta.json
/packages/*/*/reforce.js
/packages/*/*/reforce.d.ts

# @reforce/logging 的 meta 是手写源文件不是构建产物（RFC 0011 勘误，#242），由
# it/reforce-meta.spec.ts 回读自检兜底。
#
# 原因是**依赖环**，不是生成器表达不了：@Fallback() 之后 reforce lib 已经能产出 defaultBean
# （#343）。但接上生成器要 devDepend @reforce/bundler-plugin，于是
# logging → bundler-plugin → compiler →（devDep）config → logging 成环，turbo 直接拒绝构建。
# logging-pino 能自产是因为没人依赖它；logging 躺在编译器自己的依赖链下面。
#
# 真正的解法是按角色拆包（引导期设施沉到底层、starter 留在上层，见 #347），不是给生成器补能力。
!/packages/observability/logging/reforce-meta.json

# turbo
.turbo/

# test / coverage
coverage/
/e2e/e2e-junit.xml
/packages/devkit/cli/recovery-junit.xml

# logs
*.log

# os
.DS_Store

# ide
.idea/
.vscode/
!.vscode/settings.json

# env
.env
.env.*
!.env.example

# apm —— 真相源头是 apm.yml + .apm/，以下为编译/安装产物
# （以 / 开头只匹配根目录；apm.lock.yaml 同 pnpm-lock.yaml 一样需要提交，不在此列）
/AGENTS.md
/CLAUDE.md
.worktrees
.kimi-code
.agents
.claude
.codex
apm_modules/
