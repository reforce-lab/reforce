{
  "name": "@reforce/fixture-application",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "reforce dev",
    "build": "reforce build",
    "start": "reforce start",
    "typecheck": "tsc",
    "check": "biome check .",
    "check:write": "biome check --write ."
  },
  "dependencies": {
    "@reforce/config": "workspace:*",
    "@reforce/core": "workspace:*",
    "@reforce/logging": "workspace:*",
    "@reforce/web-core": "workspace:*",
    "@reforce/web-fastify": "workspace:*",
    "@reforce/web-hono": "workspace:*",
    "@reforce/web-node": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@reforce/cli": "workspace:*",
    "@reforce/tooling-tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  }
}
