pre-commit:
  parallel: true
  commands:
    biome:
      run: pnpm exec biome check --write --no-errors-on-unmatched
      stage_fixed: true


commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
